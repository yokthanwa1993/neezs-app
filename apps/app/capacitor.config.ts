import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wildbeaverroam',
  appName: 'wild-beaver-roam',
  webDir: 'dist'
};

export default config;
